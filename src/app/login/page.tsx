export default function LoginPage() {
  return (
    <main className="p-8">
      <h1 className="text-3xl font-bold text-[var(--dark-gray)] mb-4">Login</h1>
      <p className="text-[var(--dark-gray)]">Página de login — placeholder.</p>
    </main>
  );
}
